/**
 * 보고서 제한 유형을 나타내는 열거형
 * @enum {string}
 * @property {string} YEARLY - 연간 제한 유형
 * @property {string} MONTHLY - 월간 제한 유형
 * @property {string} DAILY - 일간 제한 유형
 */
export enum ReportLimitType {
  YEARLY = 'YEARLY',
  MONTHLY = 'MONTHLY',
  DAILY = 'DAILY',
}
